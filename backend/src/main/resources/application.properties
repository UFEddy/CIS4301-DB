# Application Name
spring.application.name=MLB_PlayerDB

# Oracle Database Configuration
spring.datasource.url=jdbc:oracle:thin:@oracle.cise.ufl.edu:1521:orcl
spring.datasource.username=ykazimli
spring.datasource.password=<CISE-password>
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Schema and Data Initialization
#spring.sql.init.mode=always
#spring.sql.init.schema-locations=classpath:schema.sql
#spring.sql.init.data-locations=classpath:data.sql

# Hibernate (JPA) Settings
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

# Enable JPA Logging for Debugging (Optional)
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# JWT secret key
spring.config.import=optional:environment
jwt.secret=testSecureKey123456789012345678901234567890
#jwt.secret=${JWT_SECRET:defaultSecureFallbackKey123456789012345678901234567890}